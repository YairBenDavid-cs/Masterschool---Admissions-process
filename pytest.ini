[pytest]
testpaths = tests
pythonpath = .

markers =
    system: Core FSM/engine mechanics — independent of any specific business domain or PM flow changes
    business: Masterschool-specific domain rules — score thresholds, interview decisions, contract steps
    E2E: Full end-to-end candidate journeys from registration to final admission status
